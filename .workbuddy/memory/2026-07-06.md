# 2026-07-06 工作日志

## 项目管理系统 H5 Demo — 成员编辑表格重构
- 按用户提供的 Excel 表格样式，将「编辑项目成员」改造为表格形式：
  - 序号、姓名、角色、人员属性、月份（按项目周期动态生成）、工时总计
  - 新增后台可维护角色表 `ROLES` 与人员属性枚举 `ATTRS`
  - 月份列根据项目起止时间自动生成，修改项目周期后表格列随之变化
  - 每月工时输入后工时总计实时汇总
- 修改范围：`/Users/panyejun/Downloads/01_项目管理系统/h5-demo.html`
- 涉及函数：`getMonths`、`renderMemberTable`、`showMemModal`、`showAddMemPicker`、`saveMems`、`syncMemHours`

## 角色管理模块 + chip 控件美化
- 在系统管理模块下新增「角色管理」页面（仅管理员可见），支持角色的增删改查
  - `ROLES` 从 `const` 改为 `let`，管理员可在页面直接维护
  - 新增函数：`renderRoles`、`renderRoleTable`、`addRole`、`startRoleEdit`、`saveRoleEdit`、`delRole`
- 成员编辑弹窗中的角色和人员属性控件从原生 `<select multiple>` 换为 chip/tag 风格的点击式选择器
  - 新增 chip 辅助函数：`toggleChip`、`readChips`
  - 新增 CSS：`.chip-grp`、`.chip`、`.chip.sel`、`.chip.attr-sel`
  - 角色为多选 chip（点击切换），人员属性为单选 chip（点击切换）
- 修改范围：`/Users/panyejun/Downloads/01_项目管理系统/h5-demo.html`

## 需求规格说明书更新至 V1.1
- 版本升为 V1.1，日期更新为 2026-07-06
- 新增 REQ-SM-004 角色管理需求（管理员维护角色表，增删改查即时生效）
- 完善 REQ-PM-003：成员编辑表格格式（序号/姓名/角色/人员属性/月份/工时总计）、人员属性枚举、动态月份列、chip 控件
- 更新 REQ-PM-001：手动创建进待合并清单，补充"手动创建"标签说明
- 更新 REQ-PM-007：结项合并明确为四步流程（先编辑结项信息 → 冲突检测 → 字段比对 → 确认）
- 更新 REQ-PA-001：审批拒绝增加拒绝原因必填、拒绝后其余信息仍完成调整
- 术语表新增：角色表、人员属性、Chip 选择器
- 系统管理模块从 3 项增至 4 项
